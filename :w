use strict;
use warnings;
use Data::Dumper;
use DDP;
use TOML::Tiny::Grammar;
use TOML::Tiny qw(from_toml);

my $toml = q|"String does not end here\" but ends here\\"|;
print "STR=$toml\n";

my @match = $toml =~ /($BasicChar)/g;
p @match;
